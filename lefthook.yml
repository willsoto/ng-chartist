# https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  jobs:
    - name: prettier
      glob: "*.{ts,js,md,json,html}"
      run: pnpm exec prettier --write {staged_files}
      stage_fixed: true

commit-msg:
  jobs:
    - name: commitlint
      run: pnpm exec commitlint --edit {1}
